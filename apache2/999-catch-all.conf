<VirtualHost *:80>
    # use the actual name of the host that runs Apache, e.g. vmLS5
    ServerName vgrnx22.nx-services.local
    ServerAlias *

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined

    Redirect 404 /
</VirtualHost>